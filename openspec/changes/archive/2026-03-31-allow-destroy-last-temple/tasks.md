## 1. Backend

- [x] 1.1 Rimuovere il check `registry.len() <= 1` dal comando `destroy_temple` in `commands/temple_commands.rs`

## 2. Frontend

- [x] 2.1 Rimuovere la condizione `canDestroy = temples.length > 1` nel `TempleGate` — il bottone × è sempre visibile
- [x] 2.2 Verificare che il TempleGate con lista vuota mostri correttamente solo il bottone di creazione
